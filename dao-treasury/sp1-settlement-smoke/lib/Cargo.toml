[package]
name = "sp1-settlement-smoke-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
blake2b-ref = "0.3"
hex = { version = "0.4.3", default-features = false, features = ["alloc"] }
serde = { version = "1.0", default-features = false, features = ["derive"] }
